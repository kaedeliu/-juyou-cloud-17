<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.juyou.admin.sys.mapper.UserMapper">

    <select id="findUserPermission" resultType="com.juyou.admin.sys.entity.Permission">
        SELECT
        ${ew.sqlSelect}
        FROM
        juyou_sys_user_role AS ur
        JOIN juyou_sys_role AS r ON r.role_id = ur.role_id
        JOIN juyou_sys_role_permission AS rp ON rp.role_id = r.role_id
        join juyou_sys_permission as p on p.permission_id=rp.permission_id
        ${ew.customSqlSegment}
    </select>
</mapper>
